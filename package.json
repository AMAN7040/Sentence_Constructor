{
  "name": "senconstruct",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "json-server": "json-server --watch backend/db.json --port 5001"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
